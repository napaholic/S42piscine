int (*c,p)

	if(cmp(a, b))


typedef struct	s_list
{
	int		data;
	t_list	*next;
}				t_list;



stack
linked list (a) -> (b) -> c -> d
queue

t_list *a;

(*next).data
a->data

struct s_list a;

t_list a;
t_list b;

a.next = &b;

cmp(a.next->data == b.data, )
	b.data
