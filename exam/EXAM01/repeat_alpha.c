#include <unistd.h>

void ft_putchar(char c)
{
    write(1, &c, 1);
}

int     letter_count(char c)
{
    if ('A' <= c &&)
}

void    repeat_alpha(char *str)
{
    int i;

    i = -1;
    while(str[++i])
    {
        if (str[i])
    }
}